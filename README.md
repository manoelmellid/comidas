# Comidas

PWA personal para planificar comidas y (próximamente) generar el menú semanal automáticamente, respetando reglas propias. Sin backend — todos los datos viven en IndexedDB, en tu dispositivo.

## Desarrollo

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
